{
  "name": "splitsend-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Browser-only file splitter and restorer for GitHub Pages",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate-site.mjs",
    "check": "npm run test && npm run validate"
  },
  "engines": {
    "node": ">=22"
  }
}
